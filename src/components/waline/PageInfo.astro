---
import { cn } from 'astro-pure/utils'
import Pageview from '@/components/waline/Pageview.astro'

interface Props {
  view?: boolean
  comment?: boolean
  class?: string
}

const { view, comment, class: className, ...props } = Astro.props

const path = Astro.url.pathname.endsWith('/') ? Astro.url.pathname : Astro.url.pathname + '/'
---

<div class={cn('text-base text-sm text-muted-foreground', className)} {...props}>
  <span class='waline-pageview-count' data-path={path}></span> 浏览
  {
    comment && (
      <>
        {' | '}
        <a href='#waline'>
          <span class='waline-comment-count' data-path={path} /> 评论
        </a>
      </>
    )
  }
</div>

{view && !comment && <Pageview />}

<script is:inline>
  function startAnimations() {
    var pageviewEl = document.querySelector('.waline-pageview-count')
    var commentEl = document.querySelector('.waline-comment-count')
    if (pageviewEl) CountUp.watch(pageviewEl, 1000)
    if (commentEl) CountUp.watch(commentEl, 1000)
  }

  // If Waline Comment component exists, wait for its ready signal
  var host = document.querySelector('comment-component')
  if (host) {
    host.addEventListener('waline:ready', function () {
      setTimeout(startAnimations, 300)
    }, { once: true })
  } else {
    startAnimations()
  }
</script>
